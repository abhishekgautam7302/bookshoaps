{
    "name": "assignment-book-fullstack",
    "version": "1.0.0",
    "scripts": {
        "start": "cd server && node server.js",
        "build": "cd portfolio && npm install && npm run build",
        "install-all": "cd server && npm install && cd ../portfolio && npm install"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}